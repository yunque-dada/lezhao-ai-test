# 乐造AI教务平台 - P0 登录与账号系统设计

> 设计日期：2026-03-14
> 版本：v1.0
> 状态：待审批

---

## 1. 项目概述

### 1.1 目标
开发乐造AI教务平台的P0核心功能：登录页与账号系统

### 1.2 范围
- 用户登录/注册
- 老师/学生角色区分
- 飞书多维表格用户数据存储
- 本地LocalStorage会话管理
- 密码SHA256加密

---

## 2. 技术架构

### 2.1 技术栈
| 技术 | 用途 |
|------|------|
| HTML5 | 页面结构 |
| CSS3 | 样式（响应式） |
| JavaScript ES6+ | 逻辑 |
| 飞书API | 用户数据存储 |
| LocalStorage | 会话/草稿存储 |
| Web Crypto API | SHA256密码加密 |

### 2.2 数据存储
```
┌─────────────────┐     ┌─────────────────┐
│   本地存储       │     │   飞书表格       │
│                 │     │                 │
│ - 会话Token     │     │ - 用户列表       │
│ - 草稿作品      │     │ - 班级信息       │
│ - 记住我        │     │ - 作品数据       │
└─────────────────┘     └─────────────────┘
```

---

## 3. 飞书表格配置

| 配置项 | 值 |
|--------|-----|
| App Token | IuESbAuezaGdKosDSKicR9uvnxg |
| 用户表 Table ID | tblusers |
| 班级表 Table ID | tblclasses |

---

## 4. 数据结构

### 4.1 飞书表格：用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| userId | 文本 | 用户ID（UUID） |
| username | 文本 | 用户名（唯一） |
| password | 文本 | 密码（SHA256加密） |
| role | 单选 | teacher / student |
| name | 文本 | 真实姓名 |
| classId | 文本 | 所属班级ID |
| createdAt | 时间 | 创建时间 |
| lastLogin | 时间 | 最后登录时间 |

### 4.2 飞书表格：班级表

| 字段 | 类型 | 说明 |
|------|------|------|
| classId | 文本 | 班级ID |
| className | 文本 | 班级名称 |
| teacherId | 文本 | 班主任ID |
| studentIds | 文本 | 学生ID列表（JSON） |
| createdAt | 时间 | 创建时间 |

### 4.3 本地存储

```javascript
// LocalStorage
{
  "lezhao_auth": {
    "token": "xxx",
    "userId": "user-001",
    "username": "teacher1",
    "role": "teacher",
    "name": "张老师",
    "expires": 1742000000000
  }
}
```

---

## 5. 页面设计

### 5.1 首页 (index.html)

#### 结构
```
┌─────────────────────────────────────────┐
│  [Logo]  乐造AI教务平台                │
├─────────────────────────────────────────┤
│                                         │
│     ┌───────────────────────────┐      │
│     │     🎓 老师入口           │      │
│     │     👨‍🎓 学生入口           │      │
│     └───────────────────────────┘      │
│                                         │
│     ┌───────────────────────────┐      │
│     │ 用户名  [____________]    │      │
│     │ 密码    [____________]    │      │
│     │ [ ] 记住我                │      │
│     │ [    登录    ]           │      │
│     │ 没有账号？[注册学生账号]   │      │
│     └───────────────────────────┘      │
│                                         │
├─────────────────────────────────────────┤
│  © 2026 乐造AI                         │
└─────────────────────────────────────────┘
```

### 5.2 注册页 (register.html)

#### 结构
```
┌─────────────────────────────────────────┐
│  ← 返回   注册学生账号                  │
├─────────────────────────────────────────┤
│     ┌───────────────────────────┐      │
│     │ 用户名  [____________]    │      │
│     │ 密码    [____________]    │      │
│     │ 确认密码[____________]    │      │
│     │ 姓名    [____________]    │      │
│     │ 班级    [选择 ▼]         │      │
│     │ [    注册    ]           │      │
│     │ 已有账号？[登录]         │      │
│     └───────────────────────────┘      │
└─────────────────────────────────────────┘
```

---

## 6. 功能详情

### 6.1 登录功能

| 功能 | 说明 |
|------|------|
| 角色选择 | 老师/学生 |
| 用户名登录 | 唯一标识 |
| 密码验证 | SHA256验证 |
| 记住我 | 7天免登录 |
| 错误提示 | 用户名错误/密码错误 |

### 6.2 注册功能（学生）

| 功能 | 说明 |
|------|------|
| 用户名检查 | 查重 |
| 密码确认 | 两次输入一致 |
| 班级选择 | 下拉选择 |
| 自动登录 | 注册后直接跳转工作台 |

### 6.3 会话管理

| 功能 | 说明 |
|------|------|
| Token生成 | UUID |
| 有效期 | 7天（记住我）/ 24小时 |
| 登出 | 清除本地Token |
| 自动登录 | Token有效时自动登录 |

---

## 7. 密码加密实现

使用 Web Crypto API (SHA-256)：

```javascript
// 加密密码
async function hashPassword(password) {
  const encoder = new TextEncoder();
  const data = encoder.encode(password);
  const hashBuffer = await crypto.subtle.digest('SHA-256', data);
  const hashArray = Array.from(new Uint8Array(hashBuffer));
  return hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
}

// 验证密码
async function verifyPassword(password, hashedPassword) {
  const hash = await hashPassword(password);
  return hash === hashedPassword;
}
```

---

## 8. 老师账号（初始数据）

老师账号由内部发放，初始账号预设：

| 用户名 | 密码 | 角色 | 姓名 |
|--------|------|------|------|
| teacher1 | （内部指定） | teacher | 乐造老师 |

> 注：老师账号由管理员手动添加到飞书表格

---

## 9. 页面跳转

```
index.html (登录页)
    │
    ├── 登录成功 → dashboard.html?role=teacher
    │                    └── dashboard.html?role=student
    │
    └── register.html (注册页)
              │
              └── 注册成功 → dashboard.html?role=student
```

---

## 10. 验收标准

### 10.1 功能验收
- [ ] 老师可以登录
- [ ] 学生可以登录
- [ ] 学生可以注册
- [ ] 登录失败显示错误
- [ ] 记住我功能正常
- [ ] 登出功能正常
- [ ] 页面跳转正确
- [ ] 密码SHA256加密正常

### 10.2 视觉验收
- [ ] 响应式布局（手机/平板/桌面）
- [ ] 颜色符合设计系统
- [ ] 动画流畅

---

## 11. 下一步

设计审批后，进入 **Phase 2: Writing Plans**

---

*设计完成，等待审批 - v1.0*
